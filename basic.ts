function add(n1: number, n2: number, showResult: boolean){
    return n1+ n2;


if (showResult)
{
    console.log(n1 + n2);
} else 
    return n1+ n2;
}

const number1= 5;
const number2= 2.8;
const displayResult = true;

const result = add(number1, number2, displayResult);

//console.log(result);
